--- a/net/optifine/CustomLoadingScreens.java
+++ b/net/optifine/CustomLoadingScreens.java
@@ -86,7 +86,7 @@
 
         if (ainteger.length <= 0)
         {
-            return new ImmutablePair<CustomLoadingScreen[], Integer>((Object)null, 0);
+            return new ImmutablePair<CustomLoadingScreen[], Integer>((CustomLoadingScreen[])null, 0);
         }
         else
         {
